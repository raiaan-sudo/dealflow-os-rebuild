export const spacing = {
  pageX: "clamp(1.25rem, 2.4vw, 2.5rem)",
  pageY: "clamp(1.5rem, 3vw, 3rem)",
  sectionGap: "clamp(1.25rem, 2.2vw, 2rem)",
  cardPadding: "clamp(1.25rem, 2.2vw, 2rem)",
  radius: {
    panel: "1.75rem",
    card: "1.5rem",
    control: "1rem",
  },
} as const;
